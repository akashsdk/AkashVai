import React from 'react';
import { ScrollView, View, Text, TouchableOpacity, Dimensions } from 'react-native';
const App = () => {
  return (
    <ScrollView>
      <Text style={{
        fontSize: 40,
        marginTop: 100,
        marginLeft:20,
        color:"red",
      }}>akakshhdf</Text>
    </ScrollView>
  )
}
export default App;